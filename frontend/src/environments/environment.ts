// Configuracion usada por `ng serve` (desarrollo local fuera de Docker).
// El backend corre en el host directamente en el puerto 5000.
export const environment = {
  production: false,
  apiUrl: 'http://localhost:5000/api'
};
