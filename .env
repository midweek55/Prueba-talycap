# Credenciales de desarrollo local para el contenedor de SQL Server.
# NOTA: este archivo se incluye para que "docker-compose up" funcione de inmediato.
# En un entorno real, la contrasena debe gestionarse con un mecanismo de secretos
# y este archivo no deberia estar versionado.
SA_PASSWORD=Cliente$2024Pass
